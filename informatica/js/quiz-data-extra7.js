(function () {
  if (typeof window === 'undefined') return;
  if (!window.QUIZ_DATA) window.QUIZ_DATA = {};

  const extra7 = {
    "sql": [
      { id:51, type:"mc", topic:"Window functions", text:"Cosa fa una window function come ROW_NUMBER() OVER (ORDER BY stipendio DESC) in SQL?", options:["Assegna un numero sequenziale univoco a ciascuna riga del RISULTATO, in base all'ordinamento specificato, SENZA raggruppare le righe in un'unica riga per gruppo come farebbe GROUP BY", "Conta il numero totale di righe nella tabella", "Elimina le righe duplicate", "Crea una nuova tabella fisica"], correct:0, explanation:"Le window function (introdotte in MySQL 8.0) operano su una 'finestra' di righe SENZA collassarle in un'unica riga di output (a differenza di GROUP BY); ROW_NUMBER() OVER (ORDER BY stipendio DESC) assegna 1 alla riga con stipendio più alto, 2 alla successiva, ecc., mantenendo TUTTE le righe originali nel risultato, con una colonna aggiuntiva contenente il numero di posizione." },
      { id:52, type:"mc", topic:"Window functions", text:"Cosa fa la clausola PARTITION BY in una window function, es. RANK() OVER (PARTITION BY reparto ORDER BY stipendio DESC)?", options:["Divide le righe in GRUPPI (partizioni) in base al valore della colonna specificata, e applica la funzione finestra SEPARATAMENTE all'interno di ciascuna partizione (es. una classifica indipendente per ogni reparto)", "Elimina le righe che non appartengono alla partizione specificata", "Crea una tabella temporanea per ogni partizione", "Un sinonimo di WHERE"], correct:0, explanation:"PARTITION BY reparto suddivide concettualmente le righe in gruppi in base al reparto; RANK() OVER (PARTITION BY reparto ORDER BY stipendio DESC) calcola una classifica per stipendio INDIPENDENTE per OGNI reparto (il dipendente con stipendio più alto del reparto A ha rank 1, indipendentemente da come si confronta con i dipendenti del reparto B), mantenendo tutte le righe nel risultato." },
      { id:53, type:"open", topic:"CTE", text:"Spiega cosa è una CTE (Common Table Expression, sintassi WITH nome AS (SELECT ...)) in SQL e quale vantaggio offre rispetto a una subquery annidata, con un esempio di query con CTE.", modelAnswer:"Una CTE è un RISULTATO TEMPORANEO E NOMINATO, definito con la sintassi 'WITH nome_cte AS (SELECT ...) SELECT ... FROM nome_cte ...', valido solo per la durata della query in cui è definito. ESEMPIO: 'WITH VenditeTotali AS (SELECT id_cliente, SUM(importo) AS totale FROM Ordini GROUP BY id_cliente) SELECT c.nome, vt.totale FROM Clienti c JOIN VenditeTotali vt ON c.id = vt.id_cliente WHERE vt.totale > 1000;' - la CTE 'VenditeTotali' calcola il totale ordini per cliente, e la query principale la usa come se fosse una tabella. VANTAGGI rispetto a una subquery annidata equivalente (es. 'SELECT c.nome, (SELECT SUM(importo) FROM Ordini WHERE id_cliente=c.id) AS totale FROM Clienti c WHERE ... > 1000'): (1) LEGGIBILITÀ - la query viene scomposta in 'passi' nominati, leggibili in ordine logico dall'alto verso il basso, invece di subquery annidate difficili da seguire; (2) RIUTILIZZO - una CTE può essere referenziata PIÙ VOLTE nella query principale senza ripetere la stessa subquery; (3) le CTE supportano la RICORSIVITÀ (WITH RECURSIVE), utile per gerarchie (es. organigramma aziendale, categorie ad albero), cosa non possibile con subquery normali.", keywords:["una CTE e' un risultato temporaneo nominato definito con WITH nome AS (SELECT...)","valida solo per la durata della query in cui e' definita","vantaggio: maggiore leggibilita', la query e' scomposta in passi nominati","vantaggio: riutilizzabile piu' volte nella query principale senza ripetizione","le CTE supportano la ricorsivita' (WITH RECURSIVE) per dati gerarchici"] },
      { id:54, type:"mc", topic:"Indici", text:"Perché creare un indice su OGNI colonna di una tabella NON è generalmente una buona pratica?", options:["Ogni indice aggiunto RALLENTA le operazioni di scrittura (INSERT/UPDATE/DELETE), poiché il database deve aggiornare ANCHE tutti gli indici ad ogni modifica, oltre a occupare spazio su disco aggiuntivo", "Un database può avere al massimo 2 indici per tabella", "Gli indici rendono le SELECT più lente", "Gli indici non hanno alcun costo, vanno sempre creati su tutto"], correct:0, explanation:"Gli indici accelerano le LETTURE (SELECT con WHERE/ORDER BY/JOIN sulla colonna indicizzata), ma hanno un COSTO sulle SCRITTURE: ogni INSERT/UPDATE/DELETE deve aggiornare la struttura di TUTTI gli indici della tabella (non solo i dati), aumentando il tempo di scrittura, e ogni indice occupa SPAZIO SU DISCO aggiuntivo; la scelta degli indici è quindi un COMPROMESSO basato sul pattern di utilizzo reale (colonne frequentemente usate in WHERE/JOIN/ORDER BY su tabelle con molte letture e poche scritture)." },
      { id:55, type:"mc", topic:"Transazioni", text:"Cosa significa la proprietà 'D' (Durability) nell'acronimo ACID delle transazioni?", options:["Una volta che una transazione è stata COMMITTATA con successo, le sue modifiche sono PERMANENTI e sopravvivono anche a crash del sistema (es. interruzione di corrente), tipicamente garantito tramite scrittura su disco/log delle transazioni", "La transazione deve essere eseguita rapidamente", "I dati devono essere sempre coerenti tra tabelle diverse", "Una transazione non può essere interrotta da altre transazioni"], correct:0, explanation:"Durability (Durabilità) garantisce che, dopo un COMMIT riuscito, le modifiche NON vengano PERSE, nemmeno in caso di crash improvviso del server immediatamente dopo; viene tipicamente implementata tramite un 'transaction log' (Write-Ahead Log) scritto su disco PRIMA di confermare il commit, che permette di RIPRISTINARE le modifiche durante il recovery dopo un riavvio." },
      { id:56, type:"mc", topic:"Livelli di isolamento", text:"Cosa rappresenta il livello di isolamento READ COMMITTED in una transazione SQL?", options:["Una transazione può leggere SOLO dati che sono stati COMMITTATI da altre transazioni (non legge mai 'dirty data' non confermati), ma rilegendo gli stessi dati più volte nella stessa transazione potrebbe ottenere valori DIVERSI se un'altra transazione ha fatto commit nel frattempo (non-repeatable read)", "Una transazione vede sempre uno snapshot completamente immutabile dei dati per tutta la sua durata", "Una transazione può leggere dati non confermati da altre transazioni in corso", "READ COMMITTED impedisce qualsiasi accesso concorrente al database"], correct:0, explanation:"READ COMMITTED previene il 'dirty read' (lettura di dati non confermati/rollback-abili di altre transazioni), ma NON previene il 'non-repeatable read': se la transazione A legge una riga, poi la transazione B modifica e COMMITTA quella riga, e A la rilegge, A vedrà il NUOVO valore - i dati letti possono 'cambiare' tra letture successive nella stessa transazione. Livelli più stringenti come REPEATABLE READ o SERIALIZABLE prevengono anche questo." },
      { id:57, type:"mc", topic:"JOIN avanzati", text:"Cosa restituisce un SELF JOIN, es. SELECT e.nome, m.nome AS manager FROM Dipendenti e JOIN Dipendenti m ON e.id_manager = m.id?", options:["Una query che unisce una tabella CON SE STESSA, usando due alias diversi (e, m), utile per relazioni dove una riga referenzia un'altra riga della STESSA tabella (es. dipendente-manager, categoria-sottocategoria)", "Un errore di sintassi, una tabella non può fare JOIN con se stessa", "Restituisce sempre tutte le combinazioni possibili di righe (prodotto cartesiano) senza alcun filtro", "Funziona solo se la tabella ha esattamente 2 colonne"], correct:0, explanation:"Il SELF JOIN è utile quando una tabella contiene una FOREIGN KEY che referenzia la STESSA tabella (relazione ricorsiva), es. 'Dipendenti(id, nome, id_manager)' dove id_manager referenzia id della stessa tabella; usando due ALIAS diversi (e per 'employee', m per 'manager'), si può fare JOIN tra le 'due viste' della stessa tabella per ottenere, per ogni dipendente, il nome del proprio manager (anch'esso un dipendente)." },
      { id:58, type:"mc", topic:"Normalizzazione", text:"Una tabella Ordini(id_ordine, prodotto1, prodotto2, prodotto3) con 3 colonne per memorizzare fino a 3 prodotti per ordine VIOLA quale principio di buona progettazione?", options:["Viola la Prima Forma Normale (1NF), che richiede che ogni colonna contenga un valore ATOMICO e che non vi siano gruppi RIPETUTI di colonne per rappresentare valori multipli dello stesso tipo - la soluzione è una tabella separata OrdiniProdotti(id_ordine, id_prodotto)", "Viola solo una convenzione di stile, senza problemi pratici", "È perfettamente normalizzata, nessun problema", "Viola solo la Terza Forma Normale (3NF)"], correct:0, explanation:"Avere colonne 'prodotto1, prodotto2, prodotto3' è un classico esempio di violazione della 1NF tramite 'gruppi ripetuti': cosa succede se un ordine ha 4 prodotti? Si aggiunge 'prodotto4'? Inoltre, query come 'trova tutti gli ordini con prodotto X' richiederebbero di controllare OGNI colonna prodotto1/2/3 con OR. La soluzione corretta è una tabella SEPARATA OrdiniProdotti(id_ordine, id_prodotto) con una riga per ogni combinazione ordine-prodotto, permettendo un numero ARBITRARIO di prodotti per ordine e query semplici." },
      { id:59, type:"mc", topic:"NULL", text:"Cosa restituisce l'espressione 'SELECT * FROM Tabella WHERE colonna = NULL'?", options:["NESSUNA riga, anche se 'colonna' contiene effettivamente valori NULL: il confronto con = NULL restituisce sempre NULL (non true), che viene trattato come 'falso' nella clausola WHERE; per verificare i NULL si usa IS NULL", "Tutte le righe dove colonna contiene NULL", "Un errore di sintassi", "Tutte le righe della tabella senza filtro"], correct:0, explanation:"In SQL, NULL rappresenta 'valore assente/sconosciuto', e qualsiasi confronto con NULL usando =, <>, <, > restituisce NULL (non true/false) - una sorta di 'non so'; la clausola WHERE include solo righe per cui la condizione è TRUE, quindi 'colonna = NULL' non include MAI righe, anche quelle con colonna effettivamente NULL. La sintassi corretta per verificare i NULL è 'WHERE colonna IS NULL' (o 'IS NOT NULL')." },
      { id:60, type:"open", topic:"Ottimizzazione", text:"Spiega cosa fa il comando EXPLAIN (es. EXPLAIN SELECT * FROM Ordini WHERE id_cliente = 5) in MySQL e come può aiutare a ottimizzare una query.", modelAnswer:"EXPLAIN, anteposto a una query SELECT (o UPDATE/DELETE), NON esegue effettivamente la query, ma chiede al QUERY OPTIMIZER del database di restituire il 'PIANO DI ESECUZIONE' che USEREBBE per eseguirla, SENZA eseguirla realmente. Il piano mostra informazioni come: quale/i TABELLA/E vengono accedute e in quale ORDINE; il TIPO DI ACCESSO usato per ciascuna tabella (es. 'ALL' = full table scan, che legge OGNI riga; 'ref'/'range'/'const' = uso di un indice, generalmente molto più efficiente); quale INDICE (se presente) viene effettivamente UTILIZZATO (colonna 'key'); una STIMA del numero di righe che il database prevede di esaminare (colonna 'rows'). UTILITÀ PER L'OTTIMIZZAZIONE: se EXPLAIN mostra 'type: ALL' (full table scan) su una tabella grande per una query con WHERE su una colonna NON indicizzata, questo indica che la query sta leggendo TUTTA la tabella anche se serve solo una piccola porzione - aggiungendo un INDICE su quella colonna (CREATE INDEX), e rieseguendo EXPLAIN, si dovrebbe vedere 'type' migliorare (es. 'ref' o 'range') e 'rows' diminuire drasticamente, confermando che la query ora usa l'indice in modo efficiente.", keywords:["EXPLAIN mostra il piano di esecuzione che il database USEREBBE, senza eseguire realmente la query","mostra il tipo di accesso (es. ALL = full table scan vs ref/range = uso di indice)","mostra quale indice (se presente) viene effettivamente utilizzato","mostra una stima del numero di righe esaminate","utile per identificare query lente (full scan) e verificare l'effetto dell'aggiunta di indici"] },
      { id:61, type:"mc", topic:"Chiavi", text:"Qual è la differenza tra una PRIMARY KEY e una UNIQUE KEY in SQL?", options:["Una tabella può avere SOLO UNA PRIMARY KEY (che non può contenere NULL e identifica univocamente ogni riga), ma PUÒ avere MULTIPLE UNIQUE KEY (che possono permettere un valore NULL, secondo lo standard SQL, e garantiscono comunque unicità dei valori non-NULL)", "Sono identiche in ogni aspetto", "UNIQUE KEY permette duplicati, PRIMARY KEY no", "Una tabella può avere multiple PRIMARY KEY ma solo una UNIQUE KEY"], correct:0, explanation:"PRIMARY KEY = identificatore PRINCIPALE della tabella (una sola per tabella, può essere composta da più colonne), implica NOT NULL + UNIQUE, e spesso usata come riferimento per le FOREIGN KEY di altre tabelle; UNIQUE KEY garantisce che i valori in quella colonna (o combinazione di colonne) siano UNICI tra le righe, ma una tabella può avere MULTIPLE colonne/combinazioni UNIQUE (es. sia 'email' che 'codice_fiscale' devono essere univoci, ma solo uno dei due può essere PRIMARY KEY)." },
      { id:62, type:"mc", topic:"Funzioni stringa", text:"Cosa fa la funzione CONCAT() in SQL, es. CONCAT(nome, ' ', cognome)?", options:["Unisce (concatena) più stringhe (o valori convertiti in stringa) in una singola stringa, nell'ordine specificato", "Conta il numero di caratteri in una stringa", "Divide una stringa in più parti", "Confronta due stringhe per uguaglianza"], correct:0, explanation:"CONCAT(arg1, arg2, ...) restituisce la concatenazione di tutti gli argomenti come UNA STRINGA; 'CONCAT(nome, \" \", cognome)' produce \"Mario Rossi\" da nome=\"Mario\" e cognome=\"Rossi\"; utile per costruire colonne 'calcolate' (nome completo, indirizzo formattato, ecc.) direttamente nella query SELECT." },
      { id:63, type:"mc", topic:"Backup", text:"Cosa fa il comando mysqldump in MySQL?", options:["Esporta la struttura (DDL) e/o i dati (DML, come istruzioni INSERT) di uno o più database in un file di testo (tipicamente .sql), utilizzabile per BACKUP o per trasferire/ripristinare un database", "Cancella permanentemente un database", "Crea un nuovo utente MySQL con permessi di amministratore", "Ottimizza automaticamente tutti gli indici del database"], correct:0, explanation:"mysqldump genera un file SQL contenente i comandi CREATE TABLE (struttura) e INSERT INTO (dati) necessari per RICREARE il database da zero; questo file di BACKUP può essere successivamente eseguito (es. con 'mysql nome_db < backup.sql') per RIPRISTINARE il database, su lo stesso server o su un server diverso - un meccanismo semplice ma efficace per backup logici di database di dimensioni moderate." },
      { id:64, type:"tf", topic:"Foreign Key", text:"Una FOREIGN KEY in una tabella deve sempre referenziare la PRIMARY KEY (o una colonna UNIQUE) di un'altra tabella (o della stessa tabella).", correct:true, explanation:"Vero. Una FOREIGN KEY stabilisce un vincolo di INTEGRITÀ REFERENZIALE: il valore nella colonna FK deve corrispondere a un valore EFFETTIVAMENTE ESISTENTE nella colonna referenziata della tabella 'padre' - questo è garantito SOLO se la colonna referenziata ha un vincolo di UNICITÀ (PRIMARY KEY o UNIQUE), altrimenti non sarebbe possibile garantire una corrispondenza univoca." },
      { id:65, type:"mc", topic:"GROUP BY avanzato", text:"Nella query 'SELECT reparto, COUNT(*) AS num FROM Dipendenti GROUP BY reparto', quali colonne possono essere incluse nella SELECT senza generare un errore (in modalità SQL standard/ONLY_FULL_GROUP_BY)?", options:["SOLO le colonne presenti nella clausola GROUP BY (es. 'reparto') e funzioni di AGGREGAZIONE (es. COUNT, SUM, AVG); qualsiasi altra colonna 'non aggregata e non raggruppata' (es. 'nome' di un dipendente specifico) genererebbe un errore, poiché non è chiaro QUALE valore tra le righe del gruppo dovrebbe essere mostrato", "Tutte le colonne della tabella, senza restrizioni", "Solo colonne numeriche", "Solo la colonna usata in WHERE"], correct:0, explanation:"Con GROUP BY, ogni gruppo di righe (con lo stesso valore di 'reparto') viene COLLASSATO in UNA SOLA riga di output; per colonne come 'nome' (che ha valori DIVERSI per ciascun dipendente nel gruppo), SQL non saprebbe QUALE 'nome' mostrare per quella riga di output - per questo, la modalità ONLY_FULL_GROUP_BY (default in MySQL moderno) genera un ERRORE se si seleziona una colonna che non è né nel GROUP BY né avvolta in una funzione di aggregazione." },
      { id:66, type:"mc", topic:"Operatori", text:"Cosa fa l'operatore LIKE con il carattere '%' in SQL, es. WHERE nome LIKE 'Ma%'?", options:["'%' è un carattere 'wildcard' (jolly) che corrisponde a ZERO O PIÙ caratteri QUALSIASI; 'Ma%' corrisponde a tutte le stringhe che INIZIANO con 'Ma' (es. 'Mario', 'Maria', 'Marco', anche solo 'Ma')", "'%' corrisponde a esattamente un carattere", "LIKE funziona solo con numeri", "'%' deve sempre essere alla fine del pattern"], correct:0, explanation:"In un pattern LIKE, '%' rappresenta QUALSIASI SEQUENZA di zero o più caratteri (mentre '_' rappresenta esattamente UN carattere); 'Ma%' = inizia con 'Ma'; '%aria' = termina con 'aria'; '%ari%' = contiene 'ari' in qualsiasi posizione. NOTA: query con LIKE che iniziano con '%' (es. '%aria') generalmente NON possono usare efficacemente un indice B-tree standard, risultando in un full scan." },
      { id:67, type:"open", topic:"Progettazione", text:"Spiega cosa significa 'integrità referenziale' in un database relazionale e descrivi due possibili comportamenti (ON DELETE) che si possono configurare su una FOREIGN KEY quando la riga referenziata viene eliminata.", modelAnswer:"L'INTEGRITÀ REFERENZIALE è una regola che garantisce che una FOREIGN KEY in una tabella 'figlia' non possa contenere un valore che NON corrisponde a NESSUNA riga esistente nella tabella 'padre' referenziata - cioè, non possono esistere 'riferimenti pendenti' (orfani) verso righe inesistenti. Questo viene fatto rispettare dal database stesso tramite il vincolo FOREIGN KEY. COMPORTAMENTI ON DELETE: (1) ON DELETE RESTRICT (o NO ACTION, spesso il default): il database IMPEDISCE l'eliminazione della riga 'padre' SE esistono ancora righe 'figlie' che la referenziano, restituendo un errore - utile quando l'eliminazione 'a cascata' sarebbe pericolosa (es. non si dovrebbe poter eliminare un Cliente se ha ancora Ordini associati, senza prima decidere cosa fare di quegli ordini). (2) ON DELETE CASCADE: quando la riga 'padre' viene eliminata, TUTTE le righe 'figlie' che la referenziano vengono automaticamente ELIMINATE A CASCATA - utile per relazioni 'di proprietà' forte, dove i record figli non hanno senso senza il padre (es. eliminando un Ordine, le sue righe OrdiniProdotti vengono eliminate automaticamente, poiché non avrebbe senso lasciarle 'orfane'). Esiste anche ON DELETE SET NULL, che imposta la FK delle righe figlie a NULL invece di eliminarle.", keywords:["integrita' referenziale: una FK non puo' referenziare una riga inesistente nella tabella padre","ON DELETE RESTRICT/NO ACTION: impedisce l'eliminazione del padre se esistono righe figlie","ON DELETE CASCADE: elimina automaticamente le righe figlie quando il padre viene eliminato","esempio pratico per ciascun comportamento (es. Cliente/Ordini per RESTRICT, Ordine/RigheOrdine per CASCADE)"] },
      { id:68, type:"mc", topic:"Tipi di dato", text:"Quale tipo di dato MySQL è più appropriato per memorizzare un valore booleano (vero/falso)?", options:["TINYINT(1) (o il tipo BOOLEAN, che in MySQL è un alias di TINYINT(1)), dove 0 rappresenta false e 1 (o qualsiasi valore diverso da zero) rappresenta true", "VARCHAR(255)", "TEXT", "FLOAT"], correct:0, explanation:"MySQL non ha un tipo BOOLEAN 'nativo' distinto: BOOLEAN/BOOL sono ALIAS di TINYINT(1), un intero a 1 byte che convenzionalmente contiene 0 (false) o 1 (true); usare TINYINT(1) invece di VARCHAR per valori booleani è più EFFICIENTE in termini di spazio (1 byte vs diversi byte per una stringa) e più CHIARO semanticamente nell'intento del campo." },
      { id:69, type:"mc", topic:"DISTINCT", text:"Cosa fa SELECT DISTINCT colonna1, colonna2 FROM Tabella?", options:["Restituisce le righe RIMUOVENDO le combinazioni DUPLICATE di (colonna1, colonna2): due righe sono considerate duplicate solo se ENTRAMBI i valori coincidono", "Restituisce solo la prima riga della tabella", "Ordina i risultati in ordine alfabetico", "Funziona solo con una singola colonna, non con multiple"], correct:0, explanation:"DISTINCT si applica a TUTTE le colonne elencate nella SELECT come un'unica unità: 'SELECT DISTINCT citta, regione FROM Clienti' restituisce combinazioni UNICHE di (citta, regione) - se Milano appare sia in Lombardia che (erroneamente) in un'altra regione, comparirebbero comunque DUE righe distinte, poiché la COMBINAZIONE differisce, anche se 'citta' da sola è duplicata." },
      { id:70, type:"mc", topic:"Query con date", text:"Quale funzione MySQL calcola la differenza in giorni tra due date, es. DATEDIFF(data_fine, data_inizio)?", options:["DATEDIFF()", "DATE_ADD()", "NOW()", "TIMESTAMP()"], correct:0, explanation:"DATEDIFF(data1, data2) restituisce il numero di GIORNI tra data1 e data2 (data1 - data2); es. 'DATEDIFF(\"2024-01-10\", \"2024-01-01\")' restituisce 9; utile per calcolare durate (es. giorni di permanenza, giorni di ritardo nella consegna di un ordine), confrontando colonne di tipo DATE/DATETIME." },
      { id:71, type:"mc", topic:"Trigger", text:"Quando viene eseguito un trigger AFTER UPDATE in MySQL?", options:["DOPO che una riga è stata effettivamente AGGIORNATA da una UPDATE, permettendo ad esempio di registrare la modifica in una tabella di log/audit con i nuovi valori già applicati", "PRIMA che la UPDATE venga eseguita, permettendo di MODIFICARE i valori prima del salvataggio", "Solo se la UPDATE modifica la PRIMARY KEY", "Una sola volta al giorno, indipendentemente da quante UPDATE avvengono"], correct:0, explanation:"Un trigger AFTER UPDATE si attiva DOPO che la modifica è stata applicata alla riga; può accedere sia ai valori VECCHI (OLD.colonna) sia ai NUOVI (NEW.colonna), tipico utilizzo: inserire una riga in una tabella 'AuditLog' con il vecchio e nuovo valore, per tracciare la STORIA delle modifiche. Diverso da BEFORE UPDATE, che si attiva PRIMA della modifica e permette di ALTERARE i valori che verranno effettivamente salvati (es. validazione, normalizzazione)." },
      { id:72, type:"open", topic:"JOIN multipli", text:"Scrivi una query SQL che, date le tabelle Studenti(id, nome), Iscrizioni(id_studente, id_corso) e Corsi(id, titolo), restituisce il nome dello studente e il titolo di OGNI corso a cui è iscritto, includendo ANCHE gli studenti che non sono iscritti a NESSUN corso (con titolo NULL in quel caso).", modelAnswer:"La query è: 'SELECT s.nome, c.titolo FROM Studenti s LEFT JOIN Iscrizioni i ON s.id = i.id_studente LEFT JOIN Corsi c ON i.id_corso = c.id;'. SPIEGAZIONE: si parte da Studenti come tabella 'principale' (sinistra). Il primo 'LEFT JOIN Iscrizioni i ON s.id = i.id_studente' include TUTTI gli studenti, anche quelli SENZA righe corrispondenti in Iscrizioni (per questi studenti, le colonne di 'i' saranno NULL) - questo è il motivo per cui si usa LEFT JOIN e non INNER JOIN: con INNER JOIN, gli studenti senza iscrizioni verrebbero ESCLUSI dal risultato. Il secondo 'LEFT JOIN Corsi c ON i.id_corso = c.id' collega ogni iscrizione (se presente) al corso corrispondente; se 'i.id_corso' è NULL (studente senza iscrizioni), anche 'c.titolo' risulterà NULL nel risultato finale. Il risultato include quindi: una riga per ogni combinazione studente-corso (per studenti iscritti a più corsi, appariranno più righe), PIÙ una riga (con titolo NULL) per ogni studente senza ALCUNA iscrizione.", keywords:["si parte da Studenti come tabella principale con LEFT JOIN verso Iscrizioni", "LEFT JOIN (non INNER JOIN) e' necessario per includere studenti senza iscrizioni", "secondo LEFT JOIN verso Corsi per ottenere il titolo del corso", "studenti senza iscrizioni appaiono con titolo NULL nel risultato"] },
      { id:73, type:"mc", topic:"Sicurezza", text:"Perché concatenare direttamente input dell'utente in una stringa SQL (es. \"SELECT * FROM Utenti WHERE nome = '\" + inputUtente + \"'\") è una pratica PERICOLOSA?", options:["Espone l'applicazione ad attacchi di SQL INJECTION: un attaccante può inserire come 'inputUtente' una stringa contenente SINTASSI SQL aggiuntiva (es. \"' OR '1'='1\"), alterando la LOGICA della query originale per bypassare autenticazione o estrarre/modificare dati non autorizzati", "Rende la query più lenta del 50%", "MySQL non supporta la concatenazione di stringhe", "Causa solo un avviso, non un problema di sicurezza reale"], correct:0, explanation:"Se 'inputUtente' è \"' OR '1'='1\", la query diventa \"SELECT * FROM Utenti WHERE nome = '' OR '1'='1'\" - la condizione \"'1'='1'\" è SEMPRE vera, quindi la query restituirebbe TUTTI gli utenti, indipendentemente dal nome inserito (bypass di un controllo di login, ad esempio). La difesa corretta sono le PREPARED STATEMENT (query parametrizzate), dove i parametri vengono trattati come DATI e non come codice SQL, indipendentemente dal loro contenuto." },
      { id:74, type:"mc", topic:"Funzioni numeriche", text:"Cosa fa la funzione ROUND(numero, decimali) in SQL, es. ROUND(3.14159, 2)?", options:["Arrotonda 'numero' al numero di posizioni decimali specificato; ROUND(3.14159, 2) restituisce 3.14", "Trasforma il numero in un intero troncando sempre (senza arrotondare)", "Restituisce sempre il valore assoluto del numero", "Converte il numero in una stringa"], correct:0, explanation:"ROUND(numero, decimali) arrotonda 'numero' alla precisione specificata da 'decimali' (numero di cifre dopo la virgola), usando l'arrotondamento standard (0.5 arrotonda verso l'alto); ROUND(3.14159, 2) = 3.14; ROUND(3.145, 2) = 3.15 (o 3.14 a seconda del sistema di arrotondamento esatto); utile per formattare valori monetari o percentuali per la visualizzazione." },
      { id:75, type:"tf", topic:"Indici", text:"Un indice su una colonna VARCHAR molto lunga (es. TEXT da migliaia di caratteri) può richiedere un PREFISSO (es. CREATE INDEX idx ON Tabella(colonna(50))) poiché MySQL ha limiti sulla dimensione massima della chiave di indice.", correct:true, explanation:"Vero. MySQL ha un limite sulla LUNGHEZZA TOTALE delle chiavi di indice (variabile secondo storage engine e versione, es. 3072 byte per InnoDB con certe configurazioni); per colonne TEXT/VARCHAR molto lunghe, si può creare un INDICE PARZIALE (prefix index) specificando il numero di caratteri da indicizzare, es. 'colonna(50)' indicizza solo i primi 50 caratteri - un compromesso che funziona bene se i primi N caratteri sono spesso sufficienti a distinguere i valori." },
      { id:76, type:"mc", topic:"UNION", text:"Cosa è richiesto perché UNION (o UNION ALL) tra due query SELECT funzioni correttamente?", options:["Le due SELECT devono restituire lo STESSO NUMERO di colonne, con TIPI DI DATO compatibili nelle posizioni corrispondenti (i nomi delle colonne possono differire, verrà usato quello della prima SELECT)", "Le due tabelle coinvolte devono avere lo stesso nome", "Entrambe le SELECT devono avere esattamente le stesse condizioni WHERE", "UNION funziona solo se entrambe le query interrogano la stessa tabella"], correct:0, explanation:"UNION 'impila verticalmente' i risultati di due (o più) query SELECT in un unico set di risultati; per funzionare, ogni SELECT deve produrre lo STESSO NUMERO di colonne, e le colonne nella stessa POSIZIONE devono avere tipi compatibili (es. entrambe stringhe, o entrambe numeriche) - non è richiesto che provengano dalla stessa tabella né che abbiano gli stessi nomi di colonna (l'header del risultato usa i nomi della prima SELECT)." },
      { id:77, type:"mc", topic:"Stored Procedure", text:"Quale parola chiave si usa in MySQL per ESEGUIRE (chiamare) una stored procedure precedentemente creata?", options:["CALL", "EXECUTE", "RUN", "INVOKE"], correct:0, explanation:"La sintassi per eseguire una stored procedure in MySQL è 'CALL nome_procedura(argomenti);', es. 'CALL CalcolaSconto(5, @risultato);'; le procedure possono accettare parametri IN (input), OUT (output, restituiscono un valore tramite variabili @variabile) e INOUT (entrambi)." },
      { id:78, type:"mc", topic:"Concorrenza", text:"Cosa rappresenta un 'deadlock' in un database relazionale durante transazioni concorrenti?", options:["Una situazione in cui DUE (o più) transazioni si bloccano A VICENDA in modo CIRCOLARE: la transazione A attende un lock detenuto da B, mentre B attende un lock detenuto da A - nessuna delle due può procedere; il database tipicamente rileva la situazione e ANNULLA (rollback) una delle due transazioni", "Un errore di sintassi SQL", "Un tipo di indice", "Una transazione che richiede troppo tempo per essere scritta su disco"], correct:0, explanation:"Il deadlock si verifica quando esiste un CICLO di dipendenze tra lock: A ha bloccato la riga X e sta aspettando di bloccare Y (detenuto da B), mentre B ha bloccato Y e sta aspettando di bloccare X (detenuto da A) - un'attesa circolare senza via d'uscita; il database (es. InnoDB) RILEVA periodicamente questi cicli e termina FORZATAMENTE una delle transazioni coinvolte (con un errore 'Deadlock found'), permettendo all'altra di procedere - l'applicazione deve gestire questo errore, tipicamente ri-tentando la transazione fallita." },
      { id:79, type:"open", topic:"Schema design", text:"Descrivi come progetteresti le tabelle per gestire un sistema di TAG su articoli di un blog, dove un articolo può avere più tag e un tag può essere associato a più articoli (relazione molti-a-molti).", modelAnswer:"Per una relazione MOLTI-A-MOLTI (M:N) come Articoli-Tag, servono TRE tabelle: (1) 'Articoli(id, titolo, contenuto, ...)' - contiene i dati degli articoli. (2) 'Tag(id, nome)' - contiene i tag disponibili, ciascuno memorizzato UNA SOLA VOLTA (es. 'JavaScript', 'Database', evitando duplicazione se più articoli usano lo stesso tag). (3) 'ArticoliTag(id_articolo, id_tag)' - la TABELLA DI ASSOCIAZIONE (o 'tabella ponte'/junction table): contiene una riga per OGNI combinazione articolo-tag, con 'id_articolo' FOREIGN KEY verso Articoli.id e 'id_tag' FOREIGN KEY verso Tag.id; spesso la PRIMARY KEY di questa tabella è la COMBINAZIONE (id_articolo, id_tag) (chiave primaria composta), che impedisce di associare lo stesso tag due volte allo stesso articolo. UTILIZZO: per trovare tutti i tag di un articolo, si fa JOIN tra Articoli, ArticoliTag e Tag filtrando per id_articolo; per trovare tutti gli articoli con un certo tag, si fa JOIN filtrando per il nome/id del tag. Questo schema evita la DUPLICAZIONE (un tag come stringa ripetuta in ogni articolo) e permette query efficienti in entrambe le direzioni della relazione.", keywords:["relazione molti-a-molti richiede 3 tabelle: Articoli, Tag, e una tabella di associazione (junction table)", "ArticoliTag(id_articolo, id_tag) con FK verso entrambe le tabelle", "chiave primaria composta (id_articolo, id_tag) impedisce duplicati", "evita duplicazione dei nomi dei tag e permette query efficienti in entrambe le direzioni"] },
      { id:80, type:"mc", topic:"Funzioni di aggregazione", text:"Cosa restituisce GROUP_CONCAT(nome) in MySQL applicato a un GROUP BY?", options:["Concatena i valori di 'nome' di tutte le righe di ciascun gruppo in UNA SINGOLA STRINGA, separati per default da virgola (separatore personalizzabile con SEPARATOR)", "Restituisce solo il primo valore di 'nome' del gruppo", "Conta quante volte appare ogni valore di 'nome'", "Funziona solo con valori numerici"], correct:0, explanation:"GROUP_CONCAT è specifica di MySQL: 'SELECT id_corso, GROUP_CONCAT(nome SEPARATOR \", \") FROM Iscrizioni JOIN Studenti ... GROUP BY id_corso' produrrebbe, per ogni corso, una riga con l'elenco di TUTTI i nomi degli studenti iscritti concatenati in una stringa (es. \"Mario, Anna, Luca\"), utile per report 'riassuntivi' senza dover fare elaborazione applicativa aggiuntiva." },
      { id:81, type:"mc", topic:"Operatori di confronto", text:"Cosa restituisce la clausola WHERE eta BETWEEN 18 AND 65?", options:["Le righe dove 'eta' è MAGGIORE O UGUALE a 18 E MINORE O UGUALE a 65 (l'intervallo è INCLUSIVO di entrambi gli estremi)", "Solo righe con eta esattamente uguale a 18 o 65", "Le righe dove eta è MINORE di 18 o MAGGIORE di 65 (esclusione)", "BETWEEN è equivalente a un OR senza condizioni numeriche"], correct:0, explanation:"BETWEEN valore1 AND valore2 è equivalente a 'colonna >= valore1 AND colonna <= valore2' - l'intervallo INCLUDE entrambi gli estremi (18 e 65 sono INCLUSI nel risultato); funziona anche con date e stringhe (confronto lessicografico)." },
      { id:82, type:"mc", topic:"Vincoli CHECK", text:"Cosa fa un vincolo CHECK in una definizione di tabella, es. CHECK (eta >= 0 AND eta <= 120)?", options:["Impedisce l'inserimento o l'aggiornamento di righe in cui la condizione specificata risulti FALSA, garantendo a livello di DATABASE che i valori rispettino determinati criteri (es. un range valido)", "Crea automaticamente un indice sulla colonna", "Genera un report sui valori della colonna", "Funziona solo in combinazione con FOREIGN KEY"], correct:0, explanation:"CHECK (espressione) impedisce che vengano salvate righe per cui 'espressione' è FALSE; es. 'CHECK (eta >= 0 AND eta <= 120)' impedisce di inserire eta=-5 o eta=200; questo garantisce l'integrità dei dati a livello di DATABASE, indipendentemente da QUALE applicazione (o utente con accesso diretto) inserisce i dati - una difesa più robusta della sola validazione applicativa." },
      { id:83, type:"mc", topic:"INFORMATION_SCHEMA", text:"Cosa permette di ottenere il database 'speciale' INFORMATION_SCHEMA in MySQL?", options:["Metadati SUL DATABASE stesso: elenco di tabelle, colonne, tipi di dato, vincoli, indici, ecc., interrogabili con query SQL standard (es. SELECT * FROM INFORMATION_SCHEMA.TABLES)", "Una copia di backup automatica di tutti i database", "Le password di tutti gli utenti MySQL in chiaro", "Solo statistiche sulle performance della CPU del server"], correct:0, explanation:"INFORMATION_SCHEMA è un database VIRTUALE (di sola lettura) che espone METADATI sul sistema: tabelle disponibili (INFORMATION_SCHEMA.TABLES), colonne e i loro tipi (INFORMATION_SCHEMA.COLUMNS), vincoli (KEY_COLUMN_USAGE), ecc.; utile per scrivere strumenti generici che 'esplorano' la struttura di un database, o per verificare programmaticamente se una tabella/colonna esiste." },
      { id:84, type:"open", topic:"Subquery vs JOIN", text:"Confronta l'uso di una subquery con IN e l'uso di un JOIN per ottenere lo stesso risultato: 'tutti i clienti che hanno effettuato almeno un ordine'. Quando potrebbe essere preferibile l'uno o l'altro?", modelAnswer:"APPROCCIO CON SUBQUERY (IN): 'SELECT * FROM Clienti WHERE id IN (SELECT id_cliente FROM Ordini);' - seleziona i clienti il cui id appare nella subquery, che restituisce tutti gli id_cliente distinti presenti in Ordini. APPROCCIO CON JOIN (e DISTINCT): 'SELECT DISTINCT c.* FROM Clienti c JOIN Ordini o ON c.id = o.id_cliente;' - fa il JOIN tra Clienti e Ordini (producendo una riga per OGNI ordine di ogni cliente, quindi clienti con più ordini appaiono PIÙ VOLTE), e usa DISTINCT per eliminare i duplicati. CONFRONTO: il risultato LOGICO è lo stesso (clienti con almeno un ordine), ma: (1) con JOIN, se un cliente ha 5 ordini, viene 'prodotto' temporaneamente 5 volte prima del DISTINCT - per tabelle Ordini molto grandi, questo può essere meno efficiente della subquery IN, che lavora su id DISTINTI fin dall'inizio (e i moderni optimizer spesso trasformano IN in un 'semi-join' efficiente comunque); (2) con JOIN è più naturale se si vogliono ANCHE colonne dalla tabella Ordini nel risultato finale (es. 'mostrami cliente E data del suo primo ordine'), mentre la subquery IN restituisce solo colonne di Clienti. IN GENERALE: per 'esiste almeno una corrispondenza' (senza bisogno di dati dalla tabella correlata), EXISTS o IN sono spesso più chiari/efficienti; per 'combina dati da entrambe le tabelle', JOIN è la scelta naturale.", keywords:["subquery con IN: SELECT * FROM Clienti WHERE id IN (SELECT id_cliente FROM Ordini)", "JOIN con DISTINCT produce righe duplicate (una per ordine) prima della deduplica", "JOIN e' preferibile se servono anche colonne dalla tabella correlata nel risultato", "IN/EXISTS sono spesso piu' chiari/efficienti per semplici verifiche di esistenza"] },
      { id:85, type:"mc", topic:"Permessi", text:"Cosa fa il comando GRANT SELECT ON database.* TO 'utente'@'localhost'; in MySQL?", options:["Concede all'utente 'utente' (che si connette da localhost) il privilegio di eseguire SOLO query SELECT su TUTTE le tabelle del database specificato, senza permettere INSERT/UPDATE/DELETE/DROP", "Crea un nuovo database chiamato 'utente'", "Elimina tutti i privilegi dell'utente", "Permette all'utente di connettersi da qualsiasi indirizzo IP"], correct:0, explanation:"GRANT permesso ON oggetto TO utente concede privilegi SPECIFICI; 'SELECT ON database.*' limita l'utente alla sola LETTURA di tutte le tabelle ('*') di quel database; 'utente'@'localhost' specifica che questo privilegio si applica solo quando l'utente si connette DA LOCALHOST (lo stesso privilegio potrebbe essere diverso per 'utente'@'%', che rappresenta connessioni da qualsiasi host) - questo principio del MINIMO PRIVILEGIO è importante per la sicurezza: un'applicazione che deve solo LEGGERE dati non dovrebbe avere credenziali con permessi di scrittura/eliminazione." },
      { id:86, type:"mc", topic:"Funzioni condizionali", text:"Cosa fa la funzione CASE WHEN in una SELECT, es. SELECT nome, CASE WHEN eta < 18 THEN 'Minore' ELSE 'Maggiore' END AS categoria FROM Persone?", options:["Permette di creare una colonna 'calcolata' nel risultato, il cui valore dipende da CONDIZIONI valutate per ciascuna riga (simile a un if/else applicato riga per riga)", "Filtra le righe in base alla condizione (come WHERE)", "Funziona solo all'interno di una clausola GROUP BY", "È equivalente a una JOIN condizionale"], correct:0, explanation:"CASE WHEN condizione1 THEN valore1 [WHEN condizione2 THEN valore2 ...] ELSE valoreDefault END è un'espressione condizionale valutata per OGNI RIGA: nell'esempio, ogni riga ottiene 'Minore' o 'Maggiore' in base al valore di 'eta' per QUELLA riga, in una nuova colonna 'categoria'; molto usato per CATEGORIZZARE dati numerici in fasce, tradurre codici in etichette leggibili, o creare 'colonne pivot' in combinazione con SUM/COUNT." },
      { id:87, type:"mc", topic:"Performance", text:"Perché 'SELECT *' è generalmente considerato meno efficiente di specificare esplicitamente le colonne necessarie (es. SELECT id, nome)?", options:["SELECT * trasferisce TUTTE le colonne (anche quelle non necessarie, potenzialmente grandi come campi TEXT/BLOB), aumentando il traffico di rete e la memoria utilizzata, e può impedire l'uso di 'covering index' che includerebbero solo le colonne effettivamente richieste", "SELECT * è sempre vietato dal linguaggio SQL", "Non c'è alcuna differenza di performance, solo di stile", "SELECT * restituisce le righe in ordine casuale"], correct:0, explanation:"SELECT * recupera OGNI colonna della tabella, anche se l'applicazione ne usa solo alcune - questo significa più dati trasferiti dal database all'applicazione (specialmente problematico con colonne TEXT/BLOB grandi), più memoria utilizzata, e impedisce l'ottimizzazione tramite 'covering index' (un indice che contiene TUTTE le colonne richieste dalla query, permettendo di soddisfarla SENZA accedere alla tabella principale); specificare le colonne necessarie è quindi una best practice per query performanti, oltre a rendere il codice più robusto a futuri cambiamenti dello schema (aggiunta di colonne)." },
      { id:88, type:"tf", topic:"Auto-commit", text:"In MySQL, per default (modalità autocommit attiva), ogni singola istruzione SQL (es. una UPDATE) viene automaticamente COMMITTATA come una transazione separata, a meno che non si inizi esplicitamente una transazione con START TRANSACTION o BEGIN.", correct:true, explanation:"Vero. Con autocommit=1 (default), OGNI istruzione è implicitamente racchiusa nella propria transazione, committata automaticamente al termine; per RAGGRUPPARE PIÙ istruzioni in un'UNICA transazione atomica (es. un trasferimento bancario che richiede due UPDATE), è necessario eseguire esplicitamente 'START TRANSACTION;' (o 'BEGIN;') prima delle istruzioni, e 'COMMIT;' (o 'ROLLBACK;') al termine." },
      { id:89, type:"open", topic:"Riepilogo progettazione", text:"Un sistema di e-commerce ha le tabelle Prodotti(id, nome, prezzo, quantita_disponibile) e Ordini(id, id_cliente, data). Spiega perché NON conviene memorizzare il 'prezzo' del prodotto SOLO nella tabella Prodotti per gli ordini storici, e quale soluzione adottare.", modelAnswer:"PROBLEMA: se la tabella 'DettagliOrdine' (la tabella di associazione molti-a-molti tra Ordini e Prodotti) referenzia semplicemente 'id_prodotto' e per conoscere il prezzo pagato si fa JOIN con Prodotti per leggere 'prezzo' ATTUALE, si crea un problema: i PREZZI DEI PRODOTTI CAMBIANO nel tempo (promozioni, aumenti, ecc.); se un cliente ha acquistato un prodotto a 10€ un anno fa, e oggi quel prodotto costa 15€, una query che fa JOIN con Prodotti.prezzo per calcolare il TOTALE DI QUELL'ORDINE STORICO mostrerebbe ERRONEAMENTE 15€, alterando la STORIA REALE della transazione (importante per fatturazione, contabilità, controversie con il cliente). SOLUZIONE: la tabella 'DettagliOrdine(id_ordine, id_prodotto, quantita, prezzo_unitario_al_momento_dell_ordine)' deve SALVARE UNA COPIA del prezzo AL MOMENTO dell'acquisto (uno 'snapshot' storico), oltre al riferimento id_prodotto (utile per altre informazioni del prodotto, come il nome attuale, se serve). In questo modo, il TOTALE di un ordine storico viene calcolato usando 'quantita * prezzo_unitario_al_momento_dell_ordine' della riga DettagliOrdine, rimanendo IMMUTABILE nel tempo, indipendentemente da successive modifiche al prezzo 'corrente' in Prodotti. Questo è un esempio in cui una 'leggera denormalizzazione' (duplicare il prezzo) è CORRETTA E NECESSARIA, perché rappresenta un FATTO STORICO, non un dato derivabile dallo stato attuale.", keywords:["i prezzi dei prodotti cambiano nel tempo, un JOIN con il prezzo attuale altererebbe la storia degli ordini passati", "DettagliOrdine deve salvare uno snapshot del prezzo al momento dell'ordine (prezzo_unitario)", "il totale dell'ordine storico si calcola con quantita * prezzo salvato, non il prezzo attuale", "esempio di denormalizzazione corretta e necessaria perche' rappresenta un fatto storico immutabile"] },
      { id:90, type:"mc", topic:"Operatori logici SQL", text:"Cosa restituisce 'WHERE NOT (eta < 18)' rispetto a 'WHERE eta >= 18' (assumendo che eta non possa essere NULL)?", options:["Sono LOGICAMENTE EQUIVALENTI: entrambe selezionano le righe dove eta è maggiore o uguale a 18 (NOT (eta < 18) è vero esattamente quando eta >= 18)", "NOT (eta < 18) seleziona righe con eta minore di 18", "Sono sempre diverse, NOT non può essere usato con operatori di confronto", "NOT (eta < 18) genera sempre un errore di sintassi"], correct:0, explanation:"Per valori NON-NULL, 'NOT (eta < 18)' è logicamente equivalente a 'eta >= 18' (la negazione di '<' è '>=' nei numeri); tuttavia, se 'eta' può essere NULL, le due espressioni si comportano DIVERSAMENTE rispetto ai NULL (sia 'eta < 18' che 'NOT (eta < 18)' restituirebbero NULL/falso per righe con eta NULL, escludendole comunque da entrambe le query in questo caso specifico, ma in generale NOT su espressioni con NULL richiede attenzione)." },
      { id:91, type:"mc", topic:"Replica e scalabilità", text:"In un'architettura di database con 'replica master-slave' (o primary-replica), qual è lo scopo tipico di avere uno o più server 'replica' (slave)?", options:["Distribuire il CARICO DI LETTURA (query SELECT) su più server, mentre TUTTE le scritture (INSERT/UPDATE/DELETE) avvengono sul server 'master/primary', che propaga le modifiche alle replica; utile anche per backup/disaster recovery", "Le replica permettono di scrivere dati diversi su ciascun server senza sincronizzazione", "Le replica sono usate solo per testare nuove versioni del software", "Una replica sostituisce completamente il bisogno di backup"], correct:0, explanation:"In un'architettura master-replica, tutte le SCRITTURE vanno al master, che registra le modifiche in un log (binlog in MySQL) propagato alle replica, che applicano le stesse modifiche con un piccolo ritardo (replication lag); le applicazioni possono indirizzare le LETTURE alle replica (load balancing delle query SELECT, che spesso sono la maggioranza del traffico), riducendo il carico sul master; le replica forniscono anche RIDONDANZA per disaster recovery, sebbene non sostituiscano backup veri e propri." },
      { id:92, type:"mc", topic:"Funzioni MIN/MAX", text:"Nella query 'SELECT reparto, MIN(stipendio), MAX(stipendio) FROM Dipendenti GROUP BY reparto', cosa rappresentano i risultati?", options:["Per ciascun reparto (un gruppo), lo stipendio PIÙ BASSO e PIÙ ALTO tra i dipendenti di quel reparto", "Lo stipendio minimo e massimo di TUTTA l'azienda, ripetuto per ogni riga", "Un errore, MIN e MAX non possono essere usati insieme nella stessa query", "Il numero di dipendenti con lo stipendio minimo e massimo"], correct:0, explanation:"MIN() e MAX() sono funzioni di aggregazione che, combinate con GROUP BY reparto, vengono calcolate SEPARATAMENTE per ciascun GRUPPO (reparto): la riga 'IT, 25000, 60000' significherebbe che nel reparto IT lo stipendio minimo è 25000 e il massimo è 60000; senza GROUP BY, MIN/MAX su tutta la tabella restituirebbero un SOLO valore globale." },
      { id:93, type:"open", topic:"Sicurezza avanzata", text:"Spiega cosa significa il principio del 'minimo privilegio' applicato alla configurazione degli account database usati da un'applicazione web, e perché un'applicazione NON dovrebbe connettersi al database come utente 'root' o con privilegi di amministratore completi.", modelAnswer:"Il PRINCIPIO DEL MINIMO PRIVILEGIO afferma che ogni componente di un sistema (in questo caso, l'account database usato dall'applicazione web) dovrebbe avere SOLO i permessi STRETTAMENTE NECESSARI per svolgere il proprio compito, e NULLA DI PIÙ. APPLICAZIONE AL DATABASE: un'applicazione web tipicamente necessita di SELECT, INSERT, UPDATE, DELETE su SPECIFICHE tabelle del proprio database - non necessita di permessi come DROP DATABASE, CREATE USER, GRANT, o accesso ad ALTRI database sullo stesso server. PERCHÉ NON USARE 'root'/admin: se l'applicazione si connette con un account che ha privilegi COMPLETI (incluso DROP, GRANT, accesso a tutti i database), e l'applicazione viene COMPROMESSA (es. tramite una vulnerabilità SQL injection NON completamente mitigata, o un bug che permette di eseguire query arbitrarie), l'ATTACCANTE OTTIENE gli STESSI privilegi della connessione - potendo quindi ELIMINARE INTERI DATABASE, creare nuovi utenti con privilegi elevati per mantenere accesso persistente, o accedere a dati di ALTRE applicazioni sullo stesso server database. Con un account a privilegi LIMITATI (es. solo SELECT/INSERT/UPDATE/DELETE su 3 tabelle specifiche), anche in caso di SQL injection, il DANNO POTENZIALE è CONTENUTO a quelle operazioni/tabelle - l'attaccante non potrebbe, ad esempio, eliminare il database o accedere a dati di altre applicazioni, anche se riuscisse a eseguire query arbitrarie tramite l'injection. Questo è un esempio di 'defense in depth': anche se UNA difesa (prevenzione SQL injection) fallisce, ALTRE difese (privilegi limitati) CONTENGONO il danno.", keywords:["minimo privilegio: ogni account ha solo i permessi strettamente necessari, nulla di piu'", "un'applicazione web tipicamente necessita solo SELECT/INSERT/UPDATE/DELETE su tabelle specifiche", "se l'applicazione si connette come root/admin e viene compromessa, l'attaccante ottiene privilegi completi (puo' eliminare database, creare utenti, ecc.)", "con privilegi limitati, anche in caso di SQL injection il danno potenziale e' contenuto", "esempio di 'defense in depth': piu' livelli di protezione che limitano il danno se uno fallisce"] },
      { id:94, type:"mc", topic:"Sequenze e identificatori", text:"Cosa rappresenta il valore restituito da LAST_INSERT_ID() in MySQL dopo un'istruzione INSERT su una tabella con colonna AUTO_INCREMENT?", options:["Il valore AUTO_INCREMENT generato dall'ultima INSERT eseguita dalla CONNESSIONE CORRENTE, utile per ottenere l'id della riga appena inserita senza eseguire una query SELECT separata", "Il valore massimo possibile per AUTO_INCREMENT", "Il numero totale di righe nella tabella", "Il timestamp dell'ultimo inserimento"], correct:0, explanation:"Dopo 'INSERT INTO Tabella (nome) VALUES (\"Mario\")' su una tabella con 'id INT AUTO_INCREMENT PRIMARY KEY', 'SELECT LAST_INSERT_ID()' restituisce l'id generato per QUELLA riga, nel contesto della CONNESSIONE corrente (sicuro anche con connessioni concorrenti, poiché ogni connessione vede solo il PROPRIO ultimo id generato); molto usato in applicazioni per, ad esempio, inserire un Ordine e poi usare il suo id per inserire le righe correlate in DettagliOrdine." },
      { id:95, type:"mc", topic:"Tipi JSON", text:"Cosa permette il tipo di colonna JSON, disponibile in MySQL 5.7+?", options:["Memorizzare documenti JSON (oggetti/array) direttamente in una colonna, con VALIDAZIONE automatica del formato JSON all'inserimento, e la possibilità di interrogare/estrarre VALORI SPECIFICI all'interno del documento JSON tramite funzioni dedicate (es. JSON_EXTRACT)", "È equivalente a una colonna VARCHAR, senza alcuna differenza", "Funziona solo per memorizzare numeri", "Richiede sempre una tabella separata per ogni proprietà JSON"], correct:0, explanation:"Il tipo JSON valida che il valore inserito sia JSON SINTATTICAMENTE VALIDO (a differenza di memorizzarlo come testo in VARCHAR/TEXT, che non garantirebbe validità); funzioni come JSON_EXTRACT(colonna, '$.proprieta') (o l'operatore '->') permettono di ESTRARRE valori specifici all'interno del documento per filtrare/ordinare; utile per dati SEMI-STRUTTURATI o ATTRIBUTI VARIABILI che sarebbe scomodo modellare con colonne fisse, sebbene un uso ECCESSIVO possa indicare un problema di progettazione dello schema relazionale." },
      { id:96, type:"mc", topic:"Operatori aritmetici", text:"Cosa restituisce '10 / 3' in una query SQL (assumendo tipi interi)?", options:["In MySQL, la divisione tra interi con '/' restituisce un risultato DECIMALE (es. 3.3333), a differenza della divisione INTERA di alcuni linguaggi di programmazione; per ottenere il quoziente intero si usa DIV (es. 10 DIV 3 = 3), e per il resto MOD o % (10 MOD 3 = 1)", "10 / 3 restituisce sempre 3 (troncamento automatico)", "10 / 3 genera un errore con numeri interi", "10 / 3 restituisce sempre 0"], correct:0, explanation:"A differenza di linguaggi come C/Java dove 'int / int' tronca il risultato a un intero, in SQL (MySQL) l'operatore '/' tra interi produce un risultato con DECIMALI (es. 10/3 = 3.3333); per ottenere SOLO il quoziente intero si usa l'operatore DIV ('10 DIV 3' = 3), e per il RESTO della divisione si usa MOD o '%' ('10 MOD 3' o '10 % 3' = 1)." },
      { id:97, type:"tf", topic:"Default values", text:"Una colonna definita come 'data_creazione DATETIME DEFAULT CURRENT_TIMESTAMP' viene automaticamente impostata alla data/ora corrente al momento dell'INSERT, se non viene specificato esplicitamente un valore per quella colonna.", correct:true, explanation:"Vero. DEFAULT CURRENT_TIMESTAMP è una clausola che imposta automaticamente il valore della colonna al timestamp corrente del database al momento dell'INSERT, SOLO SE la query INSERT non fornisce esplicitamente un valore per quella colonna; molto utile per campi come 'data_creazione' o 'data_registrazione', evitando che l'applicazione debba calcolare e inviare manualmente la data/ora corrente." },
      { id:98, type:"mc", topic:"Subquery nella SELECT", text:"Cosa fa una subquery utilizzata direttamente nella clausola SELECT, es. SELECT nome, (SELECT COUNT(*) FROM Ordini WHERE id_cliente = c.id) AS num_ordini FROM Clienti c?", options:["Per OGNI riga di Clienti (c), viene eseguita la subquery correlata che conta gli ordini di QUEL cliente specifico, producendo una colonna 'num_ordini' calcolata per ciascuna riga - una 'subquery correlata' eseguita ripetutamente", "La subquery viene eseguita una sola volta per tutta la query, indipendentemente dal numero di clienti", "Genera sempre un errore di sintassi", "Restituisce solo i clienti che hanno almeno un ordine"], correct:0, explanation:"Questa è una SUBQUERY CORRELATA nella SELECT: la condizione 'WHERE id_cliente = c.id' fa riferimento alla riga ESTERNA corrente (c.id), quindi la subquery viene logicamente RIESEGUITA per OGNI riga di Clienti, producendo un conteggio SPECIFICO per quel cliente; per dataset grandi, questo approccio può essere MENO efficiente di un equivalente con LEFT JOIN + GROUP BY, poiché il database potrebbe (a seconda dell'optimizer) eseguire la subquery per ciascuna delle N righe esterne." },
      { id:99, type:"open", topic:"Riepilogo SQL", text:"Riassumi il ruolo delle clausole WHERE, GROUP BY, HAVING e ORDER BY in una query SELECT, spiegando in quale ORDINE LOGICO vengono effettivamente elaborate da MySQL (anche se l'ordine di SCRITTURA nella query è diverso).", modelAnswer:"ORDINE DI SCRITTURA nella query: SELECT ... FROM ... WHERE ... GROUP BY ... HAVING ... ORDER BY ... . ORDINE DI ELABORAZIONE LOGICA (diverso!): (1) FROM - identifica le tabelle di origine (ed esegue eventuali JOIN); (2) WHERE - filtra le RIGHE INDIVIDUALI provenienti da FROM, PRIMA di qualsiasi raggruppamento - non può usare funzioni di aggregazione (SUM, COUNT, ecc.), poiché a questo punto i gruppi non esistono ancora; (3) GROUP BY - raggruppa le righe rimanenti (dopo WHERE) in gruppi in base ai valori delle colonne specificate; (4) HAVING - filtra i GRUPPI (non le righe individuali) creati da GROUP BY, e PUÒ usare funzioni di aggregazione (es. HAVING COUNT(*) > 5), poiché a questo punto i gruppi sono già stati formati; (5) SELECT - determina quali colonne/espressioni del risultato (a questo punto, righe individuali o gruppi aggregati) restituire; (6) ORDER BY - ordina il RISULTATO FINALE (dopo che SELECT ha determinato le colonne di output), può fare riferimento sia a colonne originali sia ad alias definiti in SELECT. CONSEGUENZA PRATICA: WHERE NON può filtrare su funzioni di aggregazione (es. 'WHERE COUNT(*) > 5' è un ERRORE), perché al momento di WHERE i gruppi non esistono ancora - per questo serve HAVING, che opera DOPO il raggruppamento.", keywords:["ordine di scrittura: SELECT...FROM...WHERE...GROUP BY...HAVING...ORDER BY", "ordine di elaborazione logica: FROM, WHERE, GROUP BY, HAVING, SELECT, ORDER BY", "WHERE filtra righe individuali prima del raggruppamento, non puo' usare funzioni di aggregazione", "HAVING filtra i gruppi dopo GROUP BY e puo' usare funzioni di aggregazione", "ORDER BY ordina il risultato finale dopo che SELECT ha determinato le colonne"] },
      { id:100, type:"mc", topic:"Riepilogo", text:"Quale affermazione descrive meglio il rapporto tra SQL e un database relazionale come MySQL?", options:["SQL è il linguaggio STANDARD usato per DEFINIRE la struttura dei dati (DDL: CREATE/ALTER/DROP), MANIPOLARE i dati (DML: SELECT/INSERT/UPDATE/DELETE), e CONTROLLARE l'accesso (DCL: GRANT/REVOKE) in un DBMS relazionale, che organizza i dati in TABELLE correlate tramite chiavi primarie/esterne secondo i principi della NORMALIZZAZIONE", "SQL è un linguaggio di programmazione general-purpose come Python, non legato ai database", "MySQL è un linguaggio, SQL è un database", "SQL serve solo per creare grafici e report visivi"], correct:0, explanation:"SQL (Structured Query Language) è il linguaggio attraverso cui si interagisce con un RDBMS (Relational Database Management System) come MySQL: DDL per la STRUTTURA (tabelle, vincoli, indici), DML per i DATI (CRUD), DCL per i PERMESSI; il modello RELAZIONALE organizza i dati in tabelle con RELAZIONI esplicite (FK), progettate secondo NORMALIZZAZIONE per evitare ridondanza e inconsistenze, mentre query SQL (incluse JOIN, subquery, aggregazioni, window function) permettono di RICOMPORRE e ANALIZZARE i dati secondo le necessità applicative." },
    ],
  };

  Object.keys(extra7).forEach((topic) => {
    if (!window.QUIZ_DATA[topic]) window.QUIZ_DATA[topic] = [];
    window.QUIZ_DATA[topic] = window.QUIZ_DATA[topic].concat(extra7[topic]);
  });
})();
